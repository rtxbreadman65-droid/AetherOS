[package]
name = "NetworkingOS"
version = "0.1.0"
edition = "2024"

[dependencies]

uefi = { version = "0.26", default-features = false } 

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true
